#include "binary_trees.h"

/**
* heap_extract - Finds the root node of a Max Binary Heap
* @root: Double pointer to root
* Return: value
*/

int heap_extract(heap_t **root)
{
	printf("%d", (*root)->n);
	return (0);
}
