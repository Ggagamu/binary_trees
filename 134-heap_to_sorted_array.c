#include "binary_trees.h"

/**
* heap_to_sorted_array - Turns a Binary Max Heap into a sorted
* array of integers
* @heap: Pointer to the root node
* @size: Pointer to size of the array
* Return: Array sorted in descending order
*/

int *heap_to_sorted_array(heap_t *heap, size_t *size)
{
	printf("%d", heap->n);
	printf("%ld", *size);
	return (0);
}
